angular.module('AppTasks')
    .controller('SegundaCtrl', ['$scope', '$rootScope', function($scope, $rootScope) {
      $rootScope.pageTitle = 'Segunda Questão';
      $rootScope.pageSubtitle = 'Refatoração I';
    }]);
